create database db_name
    owner = role_name
    template = template
    encoding = encoding
    lc_collate = collate
    lc_ctype = ctype
    tablespace = tablespace_name
    connection limit = max_concurrent_connection

